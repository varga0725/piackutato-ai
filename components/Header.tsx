import React from 'react';

// This component is no longer used in the new design.
// It is kept in the file system but will not be rendered by the application.
const Header: React.FC = () => {
  return null;
};

export { Header };